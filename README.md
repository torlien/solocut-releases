# SOLO CUT PRO - releases

Pacotes de atualizacao automatica do plugin SOLO CUT PRO e ferramentas de build.
Nao contem o codigo do plugin.

- `.github/workflows/whisper-mac.yml`: compila o `whisper-cli` (whisper.cpp, MIT) pra macOS.
